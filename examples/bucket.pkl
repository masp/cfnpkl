import ".../aws/s3/Bucket.pkl"

Resources {
    ["Bucket"] = new Bucket {
        Properties {
            BucketName = "mybucket"
            AccelerateConfiguration {
                AccelerationStatus = "Enable"
            }
        }
    }
}