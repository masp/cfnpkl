/// Resource Type definition for AWS::S3::BucketPolicy
extends ".../AWS/Resource.pkl"
Type = "AWS::S3::BucketPolicy"
Properties: Props?
class Props {
    /// The name of the Amazon S3 bucket to which the policy applies.
    Bucket: String?
    /// A policy document containing permissions to add to the specified
    /// bucket. In IAM, you must provide policy documents in JSON format.
    /// However, in CloudFormation you can provide the policy in JSON or YAML
    /// format because CloudFormation converts YAML to JSON before submitting
    /// it to IAM.
    PolicyDocument: Mapping | String | *Null
}
