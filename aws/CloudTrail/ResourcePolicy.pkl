/// Resource Type definition for AWS::CloudTrail::ResourcePolicy
extends ".../AWS/Resource.pkl"
Type = "AWS::CloudTrail::ResourcePolicy"
Properties: Props?
class Props {
    /// The ARN of the AWS CloudTrail resource to which the policy applies.
    ResourceArn: String?
    /// A policy document containing permissions to add to the specified
    /// resource. In IAM, you must provide policy documents in JSON format.
    /// However, in CloudFormation you can provide the policy in JSON or YAML
    /// format because CloudFormation converts YAML to JSON before submitting
    /// it to IAM.
    ResourcePolicy: Mapping | String | *Null
}
