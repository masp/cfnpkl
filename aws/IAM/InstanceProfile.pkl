/// Resource Type definition for AWS::IAM::InstanceProfile
extends ".../AWS/Resource.pkl"
Type = "AWS::IAM::InstanceProfile"
Properties: Props?
class Props {
    /// The path to the instance profile.
    Path: String?
    /// The name of the role to associate with the instance profile. Only one
    /// role can be assigned to an EC2 instance at a time, and all
    /// applications on the instance share the same role and permissions.
    Roles: Listing<String>?
    /// The name of the instance profile to create.
    InstanceProfileName: String?
    /// The Amazon Resource Name (ARN) of the instance profile.
    Arn: String?
}
