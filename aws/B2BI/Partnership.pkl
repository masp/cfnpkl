/// Definition of AWS::B2BI::Partnership Resource Type
extends ".../AWS/Resource.pkl"
Type = "AWS::B2BI::Partnership"
Properties: Props?
class Props {
    Capabilities: Listing<String(length <= 64 && length >= 1 && matches(Regex(#"^[a-zA-Z0-9_-]+$"#)))>?
    CreatedAt: String?
    Email: String(length <= 254 && length >= 5 && matches(Regex(#"^[\w\.\-]+@[\w\.\-]+$"#)))?
    ModifiedAt: String?
    Name: String(length <= 254 && length >= 1)?
    PartnershipArn: String(length <= 255 && length >= 1)?
    PartnershipId: String(length <= 64 && length >= 1 && matches(Regex(#"^[a-zA-Z0-9_-]+$"#)))?
    Phone: String(length <= 22 && length >= 7 && matches(Regex(#"^\+?([0-9 \t\-()\/]{7,})(?:\s*(?:#|x\.?|ext\.?|extension) \t*(\d+))?$"#)))?
    ProfileId: String(length <= 64 && length >= 1 && matches(Regex(#"^[a-zA-Z0-9_-]+$"#)))?
    Tags: Listing<Tag>?
    TradingPartnerId: String(length <= 64 && length >= 1 && matches(Regex(#"^[a-zA-Z0-9_-]+$"#)))?
}
class Tag {
    Key: String(length <= 128 && length >= 1)
    Value: String(length <= 256 && length >= 0)
}
